pipeline {
    agent {
        docker { image 'alpine/git:latest' }
    }
    stages {
        stage('Test') {
            steps {
                echo "I think I have figured this one out now"
            }
        }
    }
}
